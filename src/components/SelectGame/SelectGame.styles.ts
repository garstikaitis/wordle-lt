import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginTop: 20,
    flexDirection: "row",
    justifyContent: "space-between",
  },
  button: {
    padding: 8,
    marginLeft: 5,
    borderRadius: 4,
    backgroundColor: "#5749FE",
  },
  buttonText: {
    fontSize: 30,
    color: "#fff",
  },
});
